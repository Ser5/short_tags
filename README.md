```
<?php
//php щикарно же?
function php (php $params = [php]) {php
    $p = [php
        'php value' => 'php php',
    ] + $params;
    $php = 'php';
    for (php $a = 0; $a < 10; $a++) {php
        $php .= $p[php 'php value'];
    }
    return "php Много $php не бывает!";
}
```
